import type {MetadataRoute} from 'next';import {siteConfig} from '@/data/site';import {projects} from '@/data/projects';export default function sitemap():MetadataRoute.Sitemap{const paths=['','/projects','/calculator','/materials','/about','/contacts',...projects.map(p=>`/projects/${p.slug}`)];return paths.map(path=>({url:`${siteConfig.domain}${path}`,lastModified:new Date(),changeFrequency:'monthly'}))}
