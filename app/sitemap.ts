import type {MetadataRoute} from 'next';import {siteConfig} from '@/data/site';import {projects} from '@/data/projects';export const dynamic='force-static';export default function sitemap():MetadataRoute.Sitemap{const paths=['','/projects','/calculator','/materials','/about','/contacts',...projects.map(p=>`/projects/${p.slug}`)];return paths.map(path=>({url:`${siteConfig.domain}${path||'/'}${path?'/' : ''}`,lastModified:new Date(),changeFrequency:'monthly',priority:path===''?1:path==='/projects'?0.9:0.7}))}
