'use client';import Link from 'next/link';import {useState} from 'react';import {demoNote,siteConfig} from '@/data/site';
const links=[['/projects','Проекты'],['/materials','Материалы'],['/about','Как работаем'],['/calculator','Расчёт кухни']];
export function Header(){const [open,setOpen]=useState(false);return <header className="header"><Link className="logo" href="/">СЛОЙ <b>52</b></Link><button className="menuBtn" aria-expanded={open} aria-controls="nav" onClick={()=>setOpen(v=>!v)}>{open?'Закрыть':'Меню'}</button><nav id="nav" className={open?'nav open':'nav'} aria-label="Основная навигация">{links.map(([h,l])=><Link key={h} href={h} onClick={()=>setOpen(false)}>{l}</Link>)}<Link className="button small" href="/contacts" onClick={()=>setOpen(false)}>Обсудить проект</Link></nav></header>}
export function Footer(){return <footer><div><Link className="logo inverse" href="/">СЛОЙ <b>52</b></Link><p>{siteConfig.region}</p></div><nav aria-label="Навигация в подвале">{links.map(([h,l])=><Link key={h} href={h}>{l}</Link>)}<Link href="/contacts">Контакты</Link><Link href="/about">О проекте</Link></nav><p className="demoFooter">{demoNote}<br/>Все изображения и параметры используются для портфолио разработчика.</p></footer>}
