'use client';import {FormEvent,useState} from 'react';
export function DemoForm(){const [sent,setSent]=useState(false);function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();e.currentTarget.reset();setSent(true)}return <form className="contactForm" onSubmit={submit}><p className="notice">Демо-режим: данные не сохраняются и никуда не отправляются.</p><label>Имя<input name="name" required autoComplete="name"/></label><label>Способ связи<input name="contact" required placeholder="Телефон, почта или мессенджер"/></label><label>Комментарий <small>необязательно</small><textarea name="message" rows={5}/></label><button className="button" type="submit">Показать демо-ответ</button><p className="status" aria-live="polite">{sent?'Спасибо. Это локальный демонстрационный ответ - данные очищены и не были отправлены.':''}</p></form>}
