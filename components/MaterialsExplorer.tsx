'use client';import {useState} from 'react';import Link from 'next/link';import {facades,countertops,handles} from '@/data/materials';
export function MaterialsExplorer(){const [facade,setFacade]=useState('light-veneer'),[top,setTop]=useState('compact'),[handle,setHandle]=useState('profile');const query=`layout=straight&a=3000&facade=${facade}&top=${top}`;return <div className="materialsExplorer"><div className="materialPreview"><svg viewBox="0 0 700 430" role="img" aria-label="Стилизованная развёртка кухни с выбранными материалами"><rect width="700" height="430" fill="#e8e2d8"/><g className={`svgFacade ${facade}`}><rect x="80" y="80" width="540" height="280"/><path d="M80 210h540M210 80v280M350 80v280M490 80v280"/></g><rect className={`svgTop ${top}`} x="70" y="200" width="560" height="18"/><g className={`svgHandle ${handle}`}><path d="M150 232v50M290 232v50M430 232v50M570 232v50"/></g></svg><p>Условная развёртка. Цвет экрана не заменяет просмотр образца при дневном свете.</p></div><div className="materialControls"><Choice title="Фасады" items={facades} value={facade} set={setFacade}/><Choice title="Столешницы" items={countertops} value={top} set={setTop}/><Choice title="Ручки" items={handles} value={handle} set={setHandle}/><Link className="button" href={`/calculator?${query}`}>Использовать в расчёте</Link></div></div>}
function Choice({title,items,value,set}:{title:string;items:readonly {id:string;name:string;note:string;texture?:string}[];value:string;set:(v:string)=>void}){return <fieldset><legend>{title}</legend>{items.map(i=><button type="button" aria-pressed={value===i.id} key={i.id} onClick={()=>set(i.id)}>{i.texture&&<i className={`texture ${i.texture}`}/>}<span><b>{i.name}</b><small>{i.note}</small></span></button>)}</fieldset>}
